import type { Metadata, Viewport } from "next";
import { Geist } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export const metadata: Metadata = {
  title: "StoryGiraffe — Explore the World Through Untold Stories",
  description:
    "Discover geo-located audio stories tied to the places around you. Walk the streets. Hear the stories. Create your own.",
  openGraph: {
    title: "StoryGiraffe",
    description: "Geo-located audio storytelling for curious explorers.",
    url: "https://storygiraffe.com",
    siteName: "StoryGiraffe",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">
        <Navbar />
        <main className="flex-1 flex flex-col">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
